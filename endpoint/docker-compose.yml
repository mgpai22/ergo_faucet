version: "3"

services:
  faucet:
    build: .
    command: bash -c "uvicorn main:app --workers 1 --host 0.0.0.0 --port 5000"
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      NODE_URL:
      MNEMONIC: